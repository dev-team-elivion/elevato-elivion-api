components:
  parameters:
    customerId:
      name: customerId
      in: path
      required: true
      schema:
        type: string
        format: uuid

    externalRecruitmentProjectId:
      name: externalRecruitmentProjectId
      in: path
      required: true
      schema:
        type: string


    externalCandidateId:
      name: externalCandidateId
      in: path
      required: true
      schema:
        type: integer
        format: int64
